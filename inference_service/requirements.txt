# Pinned to the versions already present in the project venv.
# NOTE: install torch/torchvision FIRST from the CPU wheel index on a CPU-only
# VPS (see README) to avoid pulling the large CUDA build:
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
torchvision==0.25.0
timm==1.0.24
fastapi==0.128.1
uvicorn[standard]==0.40.0
pillow==12.1.0
numpy==2.4.2
pydantic==2.12.5
python-multipart
